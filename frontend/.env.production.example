# frontend/.env.production.example
# Copia como .env.production antes de npm run build.
# No pongas claves privadas aqui: todo VITE_* queda visible en el navegador.

VITE_API_URL=https://api.mypredicts.es
VITE_APP_NAME=MyPredicts
VITE_APP_ENV=production
