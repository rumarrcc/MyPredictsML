# ============================================================================
# REQUIREMENTS.TXT - MyPredicts Backend
# ============================================================================
# Actualizado: Abril 2026
# Python: 3.11+
#
# CORRECCIONES vs versión original:
#   - ELIMINADO pystan (incompatible con Python 3.11+, Prophet ya no lo usa)
#   - AÑADIDO cmdstanpy>=1.2.0 (nuevo backend de Stan que usa Prophet 1.1+)
#   - ELIMINADO pandas-datareader (conflictos con pandas 2.x)
#   - FIJADO yfinance>=0.2.36 (fixes de compatibilidad con pandas 2.x)
#   - ELIMINADO duplicado python-dotenv
#   - AÑADIDO gunicorn para producción
# ============================================================================

# ==================== CORE FRAMEWORK ====================
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.5.3
Flask-CORS==4.0.0
Flask-Mail==0.9.1
python-dotenv==1.0.0

# ==================== DATABASE ====================
psycopg2-binary==2.9.9
SQLAlchemy==2.0.23
alembic==1.12.1
Flask-Migrate==4.0.5

# ==================== DATA PROCESSING ====================
pandas==2.1.3
numpy==1.26.2
scipy==1.11.4

# ==================== ML / PREDICCIÓN ====================
scikit-learn==1.3.2
statsmodels==0.14.0

# Prophet 1.1.x usa cmdstanpy como backend (NO pystan)
# pystan fue deprecado — eliminado intencionalmente
cmdstanpy>=1.2.0
prophet==1.1.5

# ==================== DATOS FINANCIEROS ====================
# yfinance: usar >=0.2.58 o >=1.0.0 (ambas versiones soportadas por DataService).
# 0.2.x y 1.x tienen diferencias menores de API que DataService maneja automáticamente.
yfinance>=0.2.58

# ==================== TASK QUEUE & ASYNC ====================
celery==5.3.4
redis==5.0.1
flower==2.0.1

# ==================== VALIDACIÓN ====================
marshmallow==3.20.1
pydantic==2.5.0
jsonschema==4.20.0
dnspython==2.6.1

# ==================== UTILIDADES ====================
requests==2.31.0
python-dateutil==2.8.2
pytz==2023.3.post1
Click==8.1.7

# ==================== SERVIDOR PRODUCCIÓN ====================
gunicorn==21.2.0

# ==================== TESTING ====================
pytest==7.4.3
pytest-cov==4.1.0
pytest-flask==1.3.0
factory-boy==3.3.0

# ==================== LOGGING ====================
python-json-logger==2.0.7

# ==================== PAGOS ====================
stripe==7.9.0

# ==================== SEGURIDAD ====================
Werkzeug==3.0.1
cryptography==41.0.7

# ============================================================================
# NOTAS DE INSTALACIÓN
# ============================================================================
#
# 1. Instalar dependencias del sistema (Linux/Debian):
#    apt-get install -y gcc libpq-dev
#
# 2. Instalar requirements:
#    pip install -r requirements.txt
#
# 3. Prophet instala cmdstanpy y luego ejecuta:
#    python -c "import cmdstanpy; cmdstanpy.install_cmdstan()"
#    (solo la primera vez, descarga CmdStan ~50MB)
#
# 4. Arranque local:
#    python run.py                                   # Flask :5000
#    celery -A app.tasks.celery_app worker -l info   # Worker
#    celery -A app.tasks.celery_app beat -l info     # Scheduler
#    celery -A app.tasks.celery_app flower           # Monitor :5555
#
# 5. Docker (todo en uno):
#    docker-compose up --build
#
# ============================================================================
